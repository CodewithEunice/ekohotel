import Link from "next/link";
import React from "react";

const Hero = () => {
  return (
    <section
      className=" w-full h-[calc(100vh-240px)] md:h-[calc(100vh-60px)] mt-[40px] pt-[90px] 
      bg-hero bg-no-repeat
     bg-cover"
    >
      <div className=" container"></div>
    </section>
  );
};

export default Hero;
